USUARIO
-------
id_usuario (PK)
nombre
apellido
email
password
es_admin (boolean)
fecha_registro

1 -> N
N:1
1 -> 1

EVENTO
------
id_evento (PK)
id_creador (FK -> USUARIO.id_usuario)
nombre
descripcion
fecha_evento
fecha_creacion
activo (boolean)

1 -> N
N:1
1 -> 1

ASISTENCIA
----------
id_asistencia (PK)
id_evento (FK -> EVENTO.id_evento)
id_usuario (FK -> USUARIO.id_usuario)
estado (boolean: 'confirmado', 'cancelado')
fecha_registro




************************************************************************************
CREATE TABLE usuario(
	id_usuario SERIAL PRIMARY KEY,
	nombre VARCHAR(100) NOT NULL,
	apellido VARCHAR(100) NOT NULL,
	email VARCHAR(100) UNIQUE NOT NULL,
	ps TEXT NOT NULL,
	fecha_registro TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


CREATE TABLE evento (
	id_evento SERIAL PRIMARY KEY,
	nombre VARCHAR(100) NOT NULL,
	descripcion VARCHAR(100) NOT NULL,
	fecha_evento DATE NOT NULL,
	fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	activo BOOLEAN DEFAULT TRUE,
	id_creador INT,
	CONSTRAINT fk_id_creador FOREIGN KEY (id_creador) REFERENCES usuario (id_usuario)
);


CREATE TABLE asistencia (
	id_asistencia SERIAL PRIMARY KEY,
	id_evento INT,
	id_usuario INT,
	estado BOOLEAN DEFAULT TRUE,
	fecha_registro TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

	CONSTRAINT fk_id_evento FOREIGN KEY (id_evento) REFERENCES evento (id_evento),
	CONSTRAINT fk_id_usuario FOREIGN KEY (id_usuario) REFERENCES usuario (id_usuario)
);




INSERT INTO usuario (nombre, apellido, email, ps) VALUES
('Juan', 'Gonzalez', 'juan@gmail.com', crypt('Juan123', gen_salt('bf'))),
('Dimitri', 'Davey', 'dimitri@gmail.com', crypt('Dimitri123', gen_salt('bf'))),
('Tony', 'Stark', 'tony@gmail.com', crypt('Tony123', gen_salt('bf')));


INSERT INTO evento (id_creador, nombre, descripcion, fecha_evento) VALUES 
(1, 'Partido de FUTBOL', 'Partido entre edificio R vs Q', '2026-04-22'),
(1, 'Dia Petfriendly', 'Dia de traer a tu perro al trabajo', '2026-06-29'),
(1, 'Peinados Locos', 'Traer un peinado loco', '2026-04-30');
